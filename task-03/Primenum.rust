use std::io;

fn main() {
	let mut input = "String::new();
	println!("n= ");
	io::stdin().read_line(&mut input);
	
	for a in 2..n {
		let mut k = 0;
		for i in 2..=a/2 {
			if a%i==0 {
				k+=1;
			}
		}
		if k==0 {
			println!("{}", a);
		}
	}
}
